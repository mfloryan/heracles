
[core]
	editor = vim

[color]
	ui = auto

[color "status"]
	added = green
	changed = yellow
	untracked = red
	header = cyan

[color "branch"]
	current = yellow
	local = green
	remote = cyan

[alias]
	st = status
	last = log -1 HEAD
	who = shortlog -s --
	amend = commit --amend
	lg = log --pretty=tformat:'%h %C(yellow)%cr%Creset %cn %C(cyan)%d%Creset %Cgreen%s%Creset' --graph